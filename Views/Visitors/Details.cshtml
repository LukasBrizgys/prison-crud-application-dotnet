@model List<PrisonerVisitor>

@{
    ViewData["Title"] = "Lankytojo kaliniai";
}

<table class="table">
    <thead>
        <tr>
            <th>
                Kalinio ID
            </th>
            <th>
                Kalinio vardas
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().StartDate)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FirstOrDefault().FinishDate)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @item.Prisoner.Id
                </td>
                <td>
                    @item.Prisoner.GetFullName()
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.StartDate)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.FinishDate)
                </td>

            </tr>
        }
    </tbody>
</table>
<div>
    <a asp-action="Index">Grįžti į sąrašą</a>
</div>